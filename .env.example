# Google Analytics Measurement ID (optional)
# Only needed if using GA directly (not through GTM)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Meta Pixel ID (optional)
# Only needed if using Meta Pixel directly (not through GTM)
NEXT_PUBLIC_META_PIXEL_ID=XXXXXXXXXXXXXXX

# Note: Microsoft Clarity is managed via Google Tag Manager (GTM-WMZH965Q)
# and does not require an environment variable
